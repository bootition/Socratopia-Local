# 用户旅程审查与修复（0.1.0）

日期：2026-09-15 · 范围：下载 → 安装 → 首次启动 → 日常使用 → 升级 / 卸载

本轮用子代理做了一次完整的只读旅程走查（渲染器 22 个组件 + 主进程 IPC/存储 + 打包脚本 + installer.nsh + 5 份文档），
下面按「发现 → 处理」记录。严重度沿用审查报告：高 / 中 / 低。

## 已修复

| 严重度 | 问题 | 修复 |
|---|---|---|
| 高 | 学习进度永远为 0：`updateTextbookProgress()` 是死代码，下课只写对话产物 | 下课成功后在 `ipc/artifacts.ts` 调用 `parseCurrentPage` + `updateTextbookProgress`（页码按总页数钳制，progress.md 落盘）；新增 IPC 级回归测试 `tests/main/artifacts/artifacts-ipc.test.ts` |
| 高 | 401 提示「去设置重新填写 Key」但设置页没有填写入口，只能删 Key（无确认） | 设置页新增「更换 API Key」输入框 + 保存；删除 Key 增加确认；401 文案与入口一致 |
| 高 | 上课中切分区会静默掐断生成并丢弃已生成内容（token 白花） | 取消/离开课堂时：已生成的部分以「（已停止生成，内容未完成）」落盘保留，并在课堂显示提示条 + 「重新生成」按钮；新增回归测试 |
| 中 | 点「停止」无反馈、内容消失 | 同上（停止后保留部分内容 + 提示 + 重新生成） |
| 中 | 单价输入框无法输入小数（`1.5` 变 `15`、`0.28` 变 `28`） | 单价改为本地字符串状态 + 失焦解析提交（`PriceField`） |
| 中 | 设置保存失败时 UI 仍显示新值（磁盘满/只读时以为已保存） | 失败回滚（重新读取磁盘偏好），错误改用 `role=alert` |
| 中 | 笔记删除无确认 | 加确认弹窗；新增「取消保留笔记」测试 |
| 中 | History 里没有产物的课堂没有「生成」入口；同伴被删时主进程抛英文错误 | History 产物区新增「生成课后产物」；错误改中文；空生成修正为省略 `only`（原空数组会生成 0 项） |
| 中 | 导入教材无进度反馈、导入后无「去上课」引导 | 按钮文案「正在解析…/正在保存…」；导入成功页新增「去课堂提问 →」按钮（AppShell 接线） |
| 中 | 主进程原始错误（Zod issues / Electron 前缀）直接给用户看 | 新增 `toUserMessage()`：剥离 `Error invoking remote method…`、取 Zod `issues[0].message`、只留首行并截断；已应用到首启页/设置/导入/笔记/历史/下课/闪卡/角色列表 |
| 中 | 导出文件无成功提示也不说存哪 | 历史导出与闪卡导出后显示「已导出到浏览器下载目录：<文件名>」 |
| 中 | 切换同伴/教材会静默结束当前课堂 | 已有确认弹窗（本轮核对确认存在）；旧课在 History 中可回看 |
| 中 | 界面语言混用（导入器整块英文） | 教材导入/预览全部中文化（含错误提示、格式标签、按钮） |
| 低 | 帮助页仍写「暂不支持 PDF/EPUB/DOCX 导入」，快捷键缺 Ctrl+8/9，无版本号 | 更新为「暂不支持：扫描版 PDF（OCR）/语音/云同步/社区」；补全快捷键；页脚显示版本与数据目录 |
| 低 | `TIMEOUT/NETWORK_ERROR` 等错误码没有中文分支 | `error-messages.ts` 补全 TIMEOUT / NETWORK_ERROR / INVALID_RESPONSE / EMPTY_RESPONSE / FORBIDDEN / BUSY / NO_COMPANION / PERSIST_FAILED / LOAD_FAILED |
| 低 | 空状态无重试 | 同伴列表与教材列表空态新增「重试」按钮 |
| 低 | 100MB 文件上限与 400 万字符上限不一致，超限要在保存时才报错 | 读取文本文件后立刻校验 400 万字符并给出中文提示 |
| 低 | 15 分钟总时长上限会腰斩长回复，与「只保留空闲超时」的说明矛盾 | 移除默认总时长上限（`DEFAULT_MAX_TOTAL_MS = Infinity`），只保留「5 分钟无任何新内容」的空闲超时 |

## 文档一致性

- 统一用例数口径：README / 发布说明 / 接入检查清单 → **573 用例 / 55 文件**（恢复后套件，含本轮新增回归测试）。
- 「打包版未做应用图标」→ 已配置 `build/icon.png`，并注明未签名与 SmartScreen 处理。
- 「pages.json 只写不读」→ 已在下课时读取并写回教材进度。
- 「PDF/EPUB 后续」→ 已支持 Markdown / 文本 / PDF / EPUB / Word。
- 红队描述统一为「两轮、累计 40+ 项」。
- 分发说明只列 4 个正式产物（临时生成的 `Socratopia-Local-Setup-0.1.0.exe` 视为中间产物，不作为交付文件名）。

## 尚未处理（记录在案）

- **低 · 无障碍**：划词工具条目前只能鼠标触发（键盘/读屏用户无法使用「引用/解释/翻译/追问」）；下课确认弹窗（`role=dialog`）没有焦点陷阱、关闭后焦点不归还。
- **低 · 全局横幅**：数据目录不可写时主进程只写日志，界面没有全局提示（重启后首次写入才报错）。
- **待真机/真 Key 验证**：真实 401/402/429/断网/超时下的文案；`deepseek-v4-pro|flash` 模型 id 是否被真实 API 接受；`safeStorage` 不可用时的表现；受管控机器上的三级回退与卸载数据提示。
